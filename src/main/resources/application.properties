spring.application.name=simple-spring-app

server.port=${SERVER_PORT:8080}

spring.datasource.url=jdbc:mysql://${DB_HOST:db-mysql}:${DB_PORT:3306}/${DB_NAME:demo_docker_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.sql.init.mode=${SQL_INIT_MODE:never}
spring.sql.init.continue-on-error=true
spring.datasource.hikari.initialization-fail-timeout=${DB_INIT_FAIL_TIMEOUT:0}
